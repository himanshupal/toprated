"use client";

const Navbar = () => {
  return (
    <nav className="p-3 flex justify-between border-b border-white">
      <div className="text-lg font-semibold">Watchlist | Movie Reviews</div>
    </nav>
  );
};

export default Navbar;
